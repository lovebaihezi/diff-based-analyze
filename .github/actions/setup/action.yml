name: Set Up

description: Install Zig, Alpine, libgit2, zlib, libclang

runs:
  using: composite
  steps:
    - name: Setup Alpine
      shell: sh
      run: |
        apk update
        apk add ninja-build nodejs cmake wget tar samurai xz just make gzip bash

    - uses: goto-bus-stop/setup-zig@v2.2.0

    - name: Check Zig ENV
      shell: bash
      run: zig env

    - name: Install libclang and static binaries
      shell: bash
      run: |
        apk add clang16-dev clang16-static musl-dev libc++-dev libc++-static openssl-dev
        echo "CC=clang-16" >> $GITHUB_ENV
        echo "CXX=clang++-16" >> $GITHUB_ENV

    - name: Build libgit2, zlib
      shell: bash
      run: |
        just install-libgit2 && just build-libgit2
        just install-zlib && just build-zlib

    - name: Run zig fmt
      shell: bash
      run: zig fmt --check .



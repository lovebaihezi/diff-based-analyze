name: build deps and binary
on:
  workflow_call:
jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup
      - name: Build
        run: zig build -Doptimize=ReleaseSafe --verbose-link
      - name: Build tests
        run: |
          just config-examples
          just build-examples
